make the ajax call send the recipe object and anything else to be saved

	server side in the route

		take the object and you also req.session.userId and you save 


server side api

	then you could send the recipes to handlebars

		each recipes
			form 
				put the object in there

				input type="hidden" value="{{nameRecipe}}" name="nameOfReuoe"